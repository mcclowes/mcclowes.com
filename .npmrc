# eslint-plugin-react@7.37.5 caps its eslint peer dep at ^9.7, but the project is on
# eslint 10. The plugin works against eslint 10; this just lets npm install/ci proceed.
# Remove once eslint-plugin-react ships eslint-10 support.
legacy-peer-deps=true
